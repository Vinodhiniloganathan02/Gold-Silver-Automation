name: Schedule Test

on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test Scheduler
        run: echo "GitHub Scheduler is working!"
